import React from 'react';
import { CHeader } from '@coreui/react';

const TheHeader = () => {
  return (
    <CHeader>
      {/* Only necessary to provide the top margin to prevent overlapping of data with jhipster generated menu nav*/}
    </CHeader>
  )
}

export default TheHeader
